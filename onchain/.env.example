# Base Sepolia deploy config. Copy to .env and fill in. NEVER commit .env.
# Use a THROWAWAY testnet key only — never a wallet that holds real funds.
PRIVATE_KEY=
# Optional custom RPC; defaults to the public https://sepolia.base.org
BASE_SEPOLIA_RPC=https://sepolia.base.org
