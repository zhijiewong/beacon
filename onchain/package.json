{
  "name": "beacon-onchain",
  "version": "0.1.0",
  "private": true,
  "description": "Beacon on-chain oracle (Phase 2, sub-project A) — BeaconOracle contract + publisher",
  "license": "MIT",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "ethers": "^6.13.0",
    "chai": "^4.3.0"
  }
}
